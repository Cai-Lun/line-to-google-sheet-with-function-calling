import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // ngrok url
  allowedDevOrigins: ['spree-clergyman-symphonic.ngrok-free.dev']
};

export default nextConfig;
